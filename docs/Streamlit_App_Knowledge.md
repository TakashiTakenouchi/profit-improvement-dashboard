# Streamlit 営業利益改善AI Agentsアプリ ナレッジドキュメント

**作成日**: 2025-12-29
**最終更新**: 2026-01-07
**GitHub**: https://github.com/TakashiTakenouchi/profit-improvement-dashboard

---

## 変更履歴

| バージョン | 日付 | 変更者 | 変更内容 |
|-----------|------|--------|---------|
| 1.0.0 | 2025-12-29 | T.Takenouchi | 初版作成 |
| 2.0.0 | 2026-01-07 | Takashi.Takenouchi | 最適化アルゴリズム改良（要因ベース最適化）、Case 1-5コメント追加 |
| 2.1.0 | 2026-01-07 | Takashi.Takenouchi | 6_レポート出力.py: シナリオ1〜5と推奨アクション機能追加（画面表示/ダウンロード対応） |

---

## 1. プロジェクト概要

### 1.1 目的
恵比寿店・横浜元町店の営業利益改善を目的としたStreamlitアプリケーション。
QCストーリーに基づく分析ワークフローを提供。

### 1.2 主要機能
| ページ | 機能 | 技術 |
|--------|------|------|
| Home | ログイン・概要 | streamlit-authenticator |
| 1_現状把握 | EDAダッシュボード | Plotly, SciPy |
| 2_要因分析 | ロジスティック回帰 | scikit-learn (L1正則化) |
| 3_目標設定 | 改善目標パラメータ | Streamlit widgets |
| 4_最適化実行 | PuLP最適化 | PuLP (線形計画法) |
| 5_時系列予測 | 予測可視化 | Plotly (事前計算結果表示) |
| 6_レポート出力 | Excel/CSV出力 | openpyxl, xlsxwriter |
| 7_統計分析レポート | TOP5要因統計分析 | Markdown, Plotly |

---

## 2. ディレクトリ構造

```
streamlit_app/
├── Home.py                        # エントリーポイント（7KB）
├── pages/
│   ├── 1_現状把握.py               # EDA（VIF、正規性検定）
│   ├── 2_要因分析.py               # オッズ比TOP5抽出
│   ├── 3_目標設定.py               # 赤字月数・変動幅設定
│   ├── 4_最適化実行.py             # Before/After比較
│   ├── 5_時系列予測.py             # 信頼区間付きグラフ
│   ├── 6_レポート出力.py           # Markdown/Excelエクスポート
│   └── 7_統計分析レポート.py       # TOP5要因の統計分析・モデル比較
├── components/
│   ├── __init__.py
│   ├── auth.py                    # 簡易認証（SHA256ハッシュ）
│   ├── data_loader.py             # Excelアップロード・キャッシュ
│   └── charts.py                  # Plotlyグラフ生成関数
├── utils/
│   ├── __init__.py
│   ├── logistic.py                # ロジスティック回帰ロジック
│   └── optimization.py            # PuLP最適化ロジック
├── .streamlit/
│   ├── config.toml                # テーマ設定
│   └── secrets.toml.example       # 認証テンプレート
├── requirements.txt               # 14パッケージ
├── render.yaml                    # Renderデプロイ設定
├── .gitignore
└── README.md
```

---

## 3. 技術仕様

### 3.1 依存パッケージ (requirements.txt)
```
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
plotly>=5.18.0
openpyxl>=3.1.0
xlsxwriter>=3.1.0
japanize-matplotlib>=1.1.3
PuLP>=2.7.0
streamlit-authenticator>=0.3.2
python-dotenv>=1.0.0
scipy>=1.11.0
statsmodels>=0.14.0
seaborn>=0.12.0
```

### 3.2 認証設定
**デフォルト認証情報（components/auth.py）:**
```python
DEFAULT_CREDENTIALS = {
    "admin": "admin123",
    "user": "user123"
}
```

**本番環境（.streamlit/secrets.toml）:**
```toml
[passwords]
admin = "your_secure_password"
```

### 3.3 Render設定 (render.yaml)
```yaml
services:
  - type: web
    name: store-profit-optimizer
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run Home.py --server.port $PORT --server.address 0.0.0.0
    autoDeploy: true
```

---

## 4. コア機能の実装詳細

### 4.1 ロジスティック回帰 (utils/logistic.py)

```python
# judge列の作成
mean_profit = df['Operating_profit'].mean()
df['judge'] = (df['Operating_profit'] > mean_profit).astype(int)

# L1正則化ロジスティック回帰
model = LogisticRegression(
    penalty='l1',
    solver='saga',
    max_iter=2000,
    C=0.5,  # 正則化強度
    random_state=42
)

# オッズ比計算
odds_ratios = np.exp(model.coef_[0])
```

**除外カラム:**
```python
EXCLUDE_COLUMNS = [
    'shop', 'shop_code', 'Date', 'year', 'month',
    'Operating_profit', 'gross_profit', 'operating_cost',
    'judge', 'Total_Sales', 'discount', 'purchasing'
]
```

### 4.2 PuLP最適化 (utils/optimization.py)

#### 4.2.1 バージョン比較

| 項目 | v1.0（旧） | v2.0（新） |
|------|-----------|-----------|
| アルゴリズム | Operating_profit直接操作 | **要因ベース最適化** |
| 対応赤字月数 | 4ヶ月のみ | **0〜n_monthsまで全対応** |
| 元データ赤字月 | 処理失敗 | **正常動作** |
| gross_profit | 固定 | **調整可能** |
| 売上カテゴリ | 未対応 | **TOP5連動調整** |

#### 4.2.2 旧アルゴリズム（v1.0）の問題点

```
【問題】target_deficit_months < 4 で「最適化できません」エラー

【原因】
1. 元データに4ヶ月の赤字月が存在
2. 「黒字月」に分類しても、Operating_profitが負のまま
3. スケーリング時に負の値と正の値が混在して破綻

【例】target_deficit_months = 0 の場合
surplus_indices = [全9ヶ月]
surplus_base = [+334万, -67万, -75万, -15万, ...]  ← 負の値が混入！
scale_factor = 1704万 / (正の合計 - 負の合計)  ← 分母が小さくなる
surplus_final = [..., -88万, -61万, ...]  ← 「黒字月」なのに赤字！→ 破綻
```

#### 4.2.3 新アルゴリズム（v2.0）の定式化

```
【要因ベース最適化】

ロジック:
  1. 赤字月を選定（Operating_profitが低い順）
  2. 黒字月: gross_profitを増加させて黒字を確保（元が赤字でも強制的に黒字化）
  3. 赤字月: gross_profitを減少させて赤字を発生
  4. 年間Operating_profit合計を維持（スケーリング）
  5. 売上カテゴリ（TOP5要因）も連動して按分調整

制約:
  1. 年間Operating_profit合計を維持
  2. 黒字月は必ず正の値（最低10万円）
  3. 赤字月は必ず負の値
  4. 売上カテゴリはgross_profit比率で按分
```

**コスト項目:**
```python
COST_FIELDS = ['rent', 'personnel_expenses', 'depreciation',
               'sales_promotion', 'head_office_expenses']
```

**売上カテゴリ（v2.0で追加）:**
```python
TOP5_SALES_CATEGORIES = ["WOMEN'S_JACKETS2", "WOMEN'S_ONEPIECE",
                         'Mens_KNIT', 'Mens_PANTS']
# Note: Number_of_guestsは客数のため除外
```

#### 4.2.4 新関数（v2.0で追加）

```python
def get_factor_adjustments(df_before, df_after, target_indices) -> pd.DataFrame:
    """
    要因（売上カテゴリ）の調整内容を取得

    Returns:
        adjustments_df: 月別の売上カテゴリ変更前後比較
    """
```

### 4.3 チャート生成 (components/charts.py)

| 関数 | 用途 |
|------|------|
| `create_histogram()` | 単変量分析 |
| `create_boxplot()` | judge別分布比較 |
| `create_correlation_heatmap()` | 相関行列 |
| `create_odds_ratio_chart()` | オッズ比バーチャート |
| `create_before_after_chart()` | 改善前後比較 |
| `create_time_series_chart()` | 時系列予測（信頼区間付き） |
| `create_profit_variance_chart()` | 営業利益変動 |

### 4.4 レポート出力 (pages/6_レポート出力.py) - v2.0更新

#### 4.4.1 Case別コメント機能（v2.0で追加）

赤字月数に応じた分析コメントを自動生成する機能を追加。

```python
CASE_COMMENTS = {
    0: {"title": "Case 1: 全月黒字化シナリオ", ...},
    1: {"title": "Case 2: 赤字1ヶ月シナリオ", ...},
    2: {"title": "Case 3: 赤字2ヶ月シナリオ", ...},
    3: {"title": "Case 4: 赤字3ヶ月シナリオ", ...},
    4: {"title": "Case 5: 赤字4ヶ月シナリオ", ...}
}
```

#### 4.4.2 ケース別シナリオ詳細

| ケース | 赤字月数 | 黒字化率 | 難易度 | 概要 |
|--------|---------|---------|--------|------|
| Case 1 | 0ヶ月 | 100% | 高 | 全月黒字化（理想的だが段階的アプローチ推奨） |
| Case 2 | 1ヶ月 | 89% | 中〜高 | 6月を戦略的撤退月として位置づけ |
| Case 3 | 2ヶ月 | 78% | 中 | 5-6月の閑散期対策、繁忙期最大化 |
| Case 4 | 3ヶ月 | 67% | 低〜中 | 現状から1ヶ月改善の段階的アプローチ |
| Case 5 | 4ヶ月 | 56% | 低 | 現状維持・ベースライン確立 |

#### 4.4.3 レポート構成（v2.0）

```markdown
1. 分析概要（店舗、期間、対象月数）
2. 最適化結果
   - 改善前後比較表
   - 最適化パラメータ
3. シナリオ分析（Case別コメント）★新規
   - シナリオ概要
   - 達成のための施策
   - リスク要因
   - 推奨事項
4. 黒字化要因（ロジスティック回帰）
   - TOP5要因（オッズ比、解釈付き）★解釈追加
5. 推奨アクション
   - 短期施策（1-3ヶ月）
   - 中期施策（3-6ヶ月）
   - 長期施策（6-12ヶ月）
6. ケース別シナリオ比較表★新規
```

#### 4.4.4 レポートメニュー構成（v2.1）

`6_レポート出力.py` では以下の5種類のレポートを選択可能：

| メニュー | 機能 | 表示モード |
|---------|------|-----------|
| 📄 最適化結果レポート | 改善前後比較、Case別コメント | 画面表示 / ダウンロード(MD/Excel) |
| 🎯 シナリオ1〜5と推奨アクション | 全5シナリオの詳細・推奨アクション | 画面表示 / ダウンロード(MD/CSV) |
| 📊 統計分析サマリーレポート | TOP5要因の分布特性サマリー | 画面表示 / ダウンロード(MD) |
| 📈 統計分析詳細レポート（TOP5要因） | ヒストグラム、確率分布理論、モデル比較 | 画面表示 / ダウンロード(MD/PNG) |
| 📥 データエクスポート | ロジスティック回帰結果、月別比較データ | ダウンロード(CSV) |

#### 4.4.5 シナリオ1〜5と推奨アクション（v2.1で追加）

**画面表示モード:**
- シナリオ比較一覧表（Case 1-5）
- 各シナリオの詳細（expander形式）
  - 概要、詳細分析、推奨事項
- 推奨アクション総合サマリー
  - 短期施策（1-3ヶ月）
  - 中長期施策（3-12ヶ月）

**ダウンロードモード:**
- Markdown形式（scenario_report_YYYYMMDD.md）
- CSV形式（scenario_comparison_YYYYMMDD.csv）

### 4.5 統計分析レポート (pages/7_統計分析レポート.py)

#### 4.5.1 概要

黒字要因TOP5の確率分布特性を分析し、時系列予測モデル選定の考慮事項を表示するページ。

**到達方法:** 左サイドバーから「7_統計分析レポート」をクリック

#### 4.5.2 表示セクション

| セクション | 内容 |
|-----------|------|
| 概要 | 分析目的、対象データ、TOP5要因一覧 |
| ヒストグラム分析 | 月次・日次データの分布可視化 |
| 確率分布理論 | 負の二項分布、ゼロ過剰ポアソン分布の解説 |
| モデル比較結果 | DeepAR-NegBin vs Chronos-Bolt の実験結果 |
| 推奨アクション | カテゴリ別の推奨モデルと施策 |
| フルレポート | Top5_Factors_Analysis_Report.md の全文表示 |

#### 4.5.3 関連ファイル

```
streamlit_app/
├── docs/
│   └── Top5_Factors_Analysis_Report.md  # 統計分析レポート本体
├── output/
│   ├── top5_factors_histogram.png       # 月次データヒストグラム
│   └── daily_quantity_histogram.png     # 日次販売数量ヒストグラム
└── pages/
    └── 7_統計分析レポート.py            # 表示ページ
```

#### 4.5.4 6_レポート出力.py との連携

`6_レポート出力.py` の「📊 統計分析レポート」タブからも以下をダウンロード可能：

- Top5_Factors_Analysis_Report.md（統計分析レポート）
- top5_factors_histogram.png（月次ヒストグラム）
- daily_quantity_histogram.png（日次ヒストグラム）

---

## 5. データフロー

### 5.1 セッション状態管理
```python
st.session_state:
├── authenticated: bool          # ログイン状態
├── username: str                # ログインユーザー
├── uploaded_data: DataFrame     # アップロードデータ
├── data_source: str             # "uploaded" or "sample"
├── logistic_results: DataFrame  # オッズ比結果
├── logistic_accuracy: float     # モデル精度
├── top_factors: list            # TOP5要因
├── optimization_params: dict    # 最適化パラメータ
├── optimized_data: DataFrame    # 最適化後データ
├── optimization_summary: dict   # 最適化サマリー
└── optimization_metrics: dict   # 改善メトリクス
```

### 5.2 処理フロー
```
[ログイン] → [ファイルアップロード]
    │
    ▼
[1_現状把握] → EDA分析 → session_state保存
    │
    ▼
[2_要因分析] → ロジスティック回帰 → オッズ比TOP5
    │
    ▼
[3_目標設定] → パラメータ設定 → optimization_params保存
    │
    ▼
[4_最適化実行] → PuLP実行 → Before/After比較
    │
    ▼
[5_時系列予測] → 事前計算結果読込 → Plotlyグラフ
    │
    ▼
[6_レポート出力] → Excel/Markdown生成 → ダウンロード
    │
    ▼
[7_統計分析レポート] → TOP5要因分析 → 確率分布・モデル比較
```

---

## 6. 既存スクリプトとの対応

| 元スクリプト | 新モジュール | 変更点 |
|-------------|-------------|--------|
| `01_logistic_regression_analysis_20251227.py` | `utils/logistic.py` | 関数化、パラメータ化 |
| `05_ebisu_optimization_vol6_20251227.py` | `utils/optimization.py` | 店舗選択対応 |
| `task2_eda_dashboard.py` | `components/charts.py` | Plotly化 |
| `app/forecast_dashboard.py` | `pages/5_時系列予測.py` | 統合 |

---

## 7. 実行方法

### 7.1 ローカル実行
```bash
cd streamlit_app
pip install -r requirements.txt
streamlit run Home.py
```

### 7.2 アクセス
- URL: http://localhost:8501
- ログイン: admin / admin123

### 7.3 Renderデプロイ
1. GitHubリポジトリをRenderに接続
2. render.yaml自動検出
3. 自動デプロイ

---

## 8. 制約・注意事項

1. **AutoGluon除外**: Render無料枠（512MB）では動作不可 → 予測は事前計算結果を表示
2. **ファイルサイズ**: Streamlitアップロード最大200MB
3. **日本語ファイル名**: pagesディレクトリの日本語ファイル名はGitで正常に処理される
4. **認証**: シンプルなパスワード認証（本番はsecrets.toml使用）
5. **データ永続化**: セッション中のみ（ブラウザ閉じるとリセット）

---

## 9. トラブルシューティング

### 9.1 ModuleNotFoundError
```bash
pip install -r requirements.txt
```

### 9.2 日本語フォント
`japanize-matplotlib` がMatplotlibのグラフに日本語フォントを適用。
Plotlyは自動的に日本語対応。

### 9.3 認証エラー
`.streamlit/secrets.toml` が存在しない場合、デフォルト認証情報を使用。

### 9.4 データ読み込みエラー
必須カラム確認:
- shop, shop_code, Date
- Operating_profit, gross_profit, operating_cost
- Total_Sales

---

## 10. 今後の拡張ポイント

1. **データベース連携**: SQLite/PostgreSQLでデータ永続化
2. **ユーザー管理強化**: ロール別権限（管理者/閲覧者）
3. **リアルタイム予測**: AutoGluon APIサーバー構築
4. **通知機能**: 赤字アラートのメール/Slack通知
5. **複数店舗同時分析**: 比較ダッシュボード追加

---

## 11. テスト仕様（v2.0追加）

### 11.1 統合テスト仕様

**目的**: ワークフロー全体の整合性確認

**テスト項目:**

| # | テスト項目 | 期待結果 | 判定基準 |
|---|-----------|---------|---------|
| 1 | データ読み込み | 138行、39列 | 行数・列数一致 |
| 2 | judge列作成 | judge=1: 102件、judge=0: 36件 | 件数一致 |
| 3 | ロジスティック回帰 | 精度 > 95% | 精度閾値超過 |
| 4 | TOP5要因整合性 | 4/5要因がoptimization.pyと一致 | 一致率80%以上 |
| 5 | 最適化（Case 0-4） | 全ケース成功 | success=True |
| 6 | 年間合計維持 | 差異 < 100円 | 誤差範囲内 |

### 11.2 テストコード

```python
# 統合テスト実行スクリプト
import pandas as pd
import numpy as np
import sys
sys.path.insert(0, 'streamlit_app')

from utils.logistic import create_judge_column, run_logistic_regression, get_top_factors
from utils.optimization import run_pulp_optimization, calculate_improvement_metrics

# Step 1: データ読み込み
df = pd.read_excel('streamlit_app/data/fixed_extended_store_data_2024-FIX_kaizen_monthlyvol6_new.xlsx')
df['Date'] = pd.to_datetime(df['Date'])
df['year'] = df['Date'].dt.year
df['month'] = df['Date'].dt.month
assert len(df) == 138, f"Expected 138 rows, got {len(df)}"

# Step 2: judge列作成
df_with_judge, mean_profit = create_judge_column(df)
assert (df_with_judge['judge'] == 1).sum() > 0, "No judge=1 records"

# Step 3: ロジスティック回帰
results_df, accuracy = run_logistic_regression(df_with_judge)
assert accuracy > 0.95, f"Accuracy {accuracy} below threshold"

# Step 4: TOP5要因整合性
top5 = get_top_factors(results_df, n=5)
from utils.optimization import TOP5_SALES_CATEGORIES
overlap = set(TOP5_SALES_CATEGORIES) & set(top5['feature'].tolist())
assert len(overlap) >= 3, f"Only {len(overlap)} factors match"

# Step 5: 最適化テスト（全ケース）
ebisu = df[(df['shop_code'] == 11) & (df['year'] == 2025) & (df['month'] >= 4)]
target_indices = ebisu.index.tolist()

for target_deficit in range(5):
    df_optimized, summary = run_pulp_optimization(df.copy(), target_indices, target_deficit)
    assert summary['success'], f"Case {target_deficit} failed"
    assert abs(summary['deficit_months_after'] - target_deficit) <= 1, \
        f"Case {target_deficit}: expected {target_deficit}, got {summary['deficit_months_after']}"

# Step 6: 年間合計維持
    assert abs(summary['total_op_profit_before'] - summary['total_op_profit_after']) < 100, \
        f"Case {target_deficit}: Total not preserved"

print("ALL TESTS PASSED!")
```

### 11.3 テスト実行結果（2026-01-07）

```
======================================================================
Q1: INTEGRATION TEST - Full Workflow
======================================================================

[Step 1] Data Loading
  Total rows: 138
  Columns: 39

[Step 2] Create judge column
  Mean Operating_profit: -392,626
  judge=1 count: 102
  judge=0 count: 36

[Step 3] Logistic Regression
  Accuracy: 0.9855
  TOP5 Factors:
    1. WOMEN'S_JACKETS2: 4.5975
    2. Number_of_guests: 1.7013
    3. WOMEN'S_ONEPIECE: 1.5440
    4. Mens_KNIT: 1.5440
    5. Mens_PANTS: 1.5440

[Step 4] Target Data Selection
  Target: Ebisu 2025 Apr-Dec
  Target months: 9
  Current deficit months: 4

[Step 5] Optimization Test (All Cases)
----------------------------------------------------------------------
Case 0: target=0, result=0, total_preserved=True, PASS=True
Case 1: target=1, result=1, total_preserved=True, PASS=True
Case 2: target=2, result=2, total_preserved=True, PASS=True
Case 3: target=3, result=3, total_preserved=True, PASS=True
Case 4: target=4, result=4, total_preserved=True, PASS=True

======================================================================
Q1 RESULT: ALL TESTS PASSED!
======================================================================
```

---

## 12. v2.0 変更サマリー

### 12.1 変更ファイル

| ファイル | 変更内容 |
|---------|---------|
| `utils/optimization.py` | 要因ベース最適化アルゴリズム（v2.0）、`get_factor_adjustments()`追加 |
| `pages/6_レポート出力.py` | `CASE_COMMENTS`定義、`get_case_comment()`関数、レポート構成拡充、シナリオ1〜5メニュー追加（v2.1） |

### 12.2 GitHubコミット

```
commit 878d32d
Fix optimization algorithm to support 0-4 deficit months (v2.0)
```

### 12.3 Renderデプロイ

自動デプロイ有効（`autoDeploy: true`）のため、GitHubプッシュ後に自動更新。

---

**作成者**: T.Takenouchi
**更新者**: Takashi.Takenouchi
**最終更新**: 2026-01-07
